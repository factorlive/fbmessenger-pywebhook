import requests


# Logging toolbox 🔊
#TODO clean up logs
logging.basicConfig(level=logging.INFO)
logger = logging.getLogger("song")

class Song(url):
    def log_song(url):
        logger.info(url)



song = song(url)